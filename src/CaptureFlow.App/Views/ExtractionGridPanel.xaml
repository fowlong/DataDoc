<UserControl x:Class="CaptureFlow.App.Views.ExtractionGridPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:CaptureFlow.App.Converters">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
        <conv:InverseBoolToVisibilityConverter x:Key="InvBoolToVis" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Extraction Results" Style="{StaticResource HeaderText}" VerticalAlignment="Center" />
                <TextBlock Text="{Binding RowCount, StringFormat=' ({0} rows)'}" Style="{StaticResource SubHeaderText}"
                           VerticalAlignment="Center" Margin="4,0,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <CheckBox Content="Auto-clear" IsChecked="{Binding AutoClearOnExtract}"
                          VerticalAlignment="Center" Margin="0,0,8,0"
                          ToolTip="When checked, clears previous results on each extraction. Uncheck to accumulate results." />
                <Button Content="Delete Row" Style="{StaticResource ToolBarButton}"
                        Command="{Binding DeleteSelectedRowsCommand}"
                        CommandParameter="{Binding SelectedItems, ElementName=ResultsGrid}"
                        Margin="0,0,4,0" />
                <Button Content="Add Row" Style="{StaticResource ToolBarButton}" Command="{Binding AddRowCommand}" Margin="0,0,4,0" />
                <Button Content="Clear" Style="{StaticResource ToolBarButton}" Command="{Binding ClearResultsCommand}" />
            </StackPanel>
        </Grid>

        <!-- CSV Group Tabs (shown when multiple groups exist) -->
        <ListBox Grid.Row="1" ItemsSource="{Binding CsvGroups}"
                 SelectedItem="{Binding SelectedCsvGroup}"
                 Margin="0,0,0,4"
                 BorderThickness="0" Background="Transparent"
                 Visibility="{Binding HasResults, Converter={StaticResource BoolToVis}}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd" Background="{StaticResource PanelBackgroundBrush}"
                                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,1,1,0"
                                        CornerRadius="4,4,0,0" Padding="10,4" Margin="0,0,2,0" Cursor="Hand">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceBrush}" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- Data Grid -->
        <DataGrid Grid.Row="2"
                  x:Name="ResultsGrid"
                  ItemsSource="{Binding ResultsTable}"
                  AutoGenerateColumns="True"
                  CanUserAddRows="True"
                  CanUserDeleteRows="True"
                  CanUserSortColumns="True"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="#F8FAFC"
                  HorizontalGridLinesBrush="#E2E8F0"
                  VerticalGridLinesBrush="#E2E8F0"
                  BorderThickness="1"
                  BorderBrush="{StaticResource BorderBrush}"
                  FontSize="12"
                  RowHeaderWidth="0"
                  ClipboardCopyMode="IncludeHeader"
                  Visibility="{Binding HasResults, Converter={StaticResource BoolToVis}}">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}" />
                    <Setter Property="Padding" Value="8,4" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="FontSize" Value="12" />
                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
                    <Setter Property="BorderThickness" Value="0,0,1,1" />
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>

        <!-- Empty state -->
        <TextBlock Grid.Row="2" Text="Run extraction to see results here"
                   Visibility="{Binding HasResults, Converter={StaticResource InvBoolToVis}}"
                   Style="{StaticResource SubHeaderText}"
                   HorizontalAlignment="Center" VerticalAlignment="Center" />

        <!-- Footer -->
        <TextBlock Grid.Row="3" Margin="0,4,0,0"
                   Text="{Binding SourceFileName, StringFormat='Source: {0}'}"
                   Style="{StaticResource SubHeaderText}" />
    </Grid>
</UserControl>
